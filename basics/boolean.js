// === - equality operator
// !== - not equal operator
// < - less than operatot
// > - greater than operator
// <= -  less than or equal
// >= - greater than or equal 
let temp = 180
let isFreezing = temp <= 32

if (isFreezing) {
    console.log('It is freezing outside!')
}

if (temp >= 110) {
    console.log('It is way to hot outside!')
    
}
// Challege area

// creat age set to your age
// calculate is chld - if they are 7 or under
// calculate is senior - if they are  65 or older
//print is child value
//print is senior value

let age = 5

//if 7 or under print message about child prcicing
// if 65 or older print message about senior pricing

if (age <= 7) {
    console.log('You qualify for a child ticket price')
} 

if (age >=  65) {
    console.log('You qualify for our senior ticket price')
}